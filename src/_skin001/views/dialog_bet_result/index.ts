import DialogMount from "@/core/global/DialogMount";
import getProxy from "@/core/global/getProxy";
import DialogBetResultProxy from "./proxy/DialogBetResultProxy";
import DialogBetResult from "./views/DialogBetResult.vue";

function show() {
    // DialogMount(DialogBetResult);
    // const proxy: DialogBetResultProxy = getProxy(DialogBetResultProxy);
    // proxy.pageData.bShow = true;
}

export default { show };
